{
  "name": "youtube-watched-marker",
  "version": "0.1.4",
  "private": true,
  "description": "Firefox-first WebExtension for queueing YouTube videos and simulating watched playback.",
  "scripts": {
    "check": "node --check src/background.js && node --check src/player-worker.js && node --check src/youtube-content.js && node --check popup/popup.js",
    "lint": "web-ext lint --source-dir . --ignore-files package.json package-lock.json node_modules web-ext-artifacts",
    "build": "web-ext build --source-dir . --artifacts-dir web-ext-artifacts --overwrite-dest --ignore-files package.json package-lock.json node_modules web-ext-artifacts README.md"
  },
  "devDependencies": {
    "web-ext": "^8.9.0"
  }
}
